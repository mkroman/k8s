apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prometheus-operator-admission-webhook
  namespace: prometheus-operator
spec:
  dnsNames:
    - admission-webhook.prometheus-operator.svc
  secretName: admission-webhook-certs
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
